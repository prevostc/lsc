foundry
foundry-lean-erc20-demo

.lake
external/EVMYulLean/

# Scratch (difftest JSON dumps, old exporter). Tracked artifacts live in
# Examples/*/compiled/ and are written there by scripts/export_bytecode.sh.
out/
