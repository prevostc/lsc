# Token example

Minimal IERC20-shaped LSC contract with proved safety theorems and a [`HonestERC20`](../../Lsc/Lib/Interfaces/IERC20.lean) witness.

```bash
cd examples/token && lake build Token TokenProofs TokenTheorem
```

See [`docs/reference/TOKEN.md`](../../docs/reference/TOKEN.md).
